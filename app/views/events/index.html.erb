<h1>Listing events</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Address</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @events.each do |event| %>
  <tr>
    <td><%= event.name %></td>
    <td><%= event.description %></td>
    <td><%= event.address %></td>
    <td><%= link_to 'Show', [event.user, event] %></td>
    <td><%= link_to 'Edit', edit_user_event_path(event.user, event) %></td>
    <td><%= link_to 'Destroy', [event.user, event], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event', new_user_event_path(params[:user_id]) %>
